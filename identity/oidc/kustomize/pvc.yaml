kind: PersistentVolumeClaim 
apiVersion: v1
metadata:
  name: oidc-postgres-claim
spec:
  storageClassName: sdd-hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2G